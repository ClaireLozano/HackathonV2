$(document).ready(function () {

    /**
     * Init
     *
     * @return
     */
    function init() {

    };

    /**
     * Add bind listeners
     *
     * @return
     */
    var bindListeners = function () {

    };

    init();
    bindListeners();
});
